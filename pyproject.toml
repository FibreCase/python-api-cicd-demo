[project]
name = "python-api-cicd-demo"
version = "0.1.0"
description = "This is a demo project for Python API CI/CD."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
test = [
	"pytest>=8",
]
